# Python
python-dotenv~=1.0.0

# Django
#  Django 4.2 is throwing warnings:
#  "StreamingHttpResponse must consume synchronous iterators in order to serve them asynchronously. ..."
Django==4.1
djangorestframework

# Database
psycopg2-binary
dj_database_url

# Security
django-cors-headers

# Deployment
gunicorn
uvicorn[standard]~=0.21.1
whitenoise

# Distributed async tasks
celery~=5.2.7
redis
django-celery-beat
django-celery-results

# Channels
channels
channels_redis
